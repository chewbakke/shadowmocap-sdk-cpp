name: shadowmocap-sdk-cpp CI

on:
  push:
      branches: [ main ]

jobs:
  build-and-test:
      runs-on: ubuntu-latest
      steps:
      - uses: actions/checkout@v2
      - name: make
      run: make
      - name: make test
      run: make test
      with:
        submodule_update: ON